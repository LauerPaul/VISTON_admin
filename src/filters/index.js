export default {
	uppercase: function(v) {
	  return v.toUpperCase();
	},
	capitalize: function(v) {
	  return v.toCapitalize();
	},
	lowercase: function(v) {
	  return v.toLowercase();
	},
	currency: function(v) {
	  return v.toCurrency();
	}
}